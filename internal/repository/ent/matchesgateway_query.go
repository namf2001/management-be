// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"management-be/internal/repository/ent/matchesgateway"
	"management-be/internal/repository/ent/predicate"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// MatchesGatewayQuery is the builder for querying MatchesGateway entities.
type MatchesGatewayQuery struct {
	config
	ctx        *QueryContext
	order      []matchesgateway.OrderOption
	inters     []Interceptor
	predicates []predicate.MatchesGateway
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MatchesGatewayQuery builder.
func (mgq *MatchesGatewayQuery) Where(ps ...predicate.MatchesGateway) *MatchesGatewayQuery {
	mgq.predicates = append(mgq.predicates, ps...)
	return mgq
}

// Limit the number of records to be returned by this query.
func (mgq *MatchesGatewayQuery) Limit(limit int) *MatchesGatewayQuery {
	mgq.ctx.Limit = &limit
	return mgq
}

// Offset to start from.
func (mgq *MatchesGatewayQuery) Offset(offset int) *MatchesGatewayQuery {
	mgq.ctx.Offset = &offset
	return mgq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mgq *MatchesGatewayQuery) Unique(unique bool) *MatchesGatewayQuery {
	mgq.ctx.Unique = &unique
	return mgq
}

// Order specifies how the records should be ordered.
func (mgq *MatchesGatewayQuery) Order(o ...matchesgateway.OrderOption) *MatchesGatewayQuery {
	mgq.order = append(mgq.order, o...)
	return mgq
}

// First returns the first MatchesGateway entity from the query.
// Returns a *NotFoundError when no MatchesGateway was found.
func (mgq *MatchesGatewayQuery) First(ctx context.Context) (*MatchesGateway, error) {
	nodes, err := mgq.Limit(1).All(setContextOp(ctx, mgq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{matchesgateway.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mgq *MatchesGatewayQuery) FirstX(ctx context.Context) *MatchesGateway {
	node, err := mgq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MatchesGateway ID from the query.
// Returns a *NotFoundError when no MatchesGateway ID was found.
func (mgq *MatchesGatewayQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mgq.Limit(1).IDs(setContextOp(ctx, mgq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{matchesgateway.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mgq *MatchesGatewayQuery) FirstIDX(ctx context.Context) int {
	id, err := mgq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MatchesGateway entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MatchesGateway entity is found.
// Returns a *NotFoundError when no MatchesGateway entities are found.
func (mgq *MatchesGatewayQuery) Only(ctx context.Context) (*MatchesGateway, error) {
	nodes, err := mgq.Limit(2).All(setContextOp(ctx, mgq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{matchesgateway.Label}
	default:
		return nil, &NotSingularError{matchesgateway.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mgq *MatchesGatewayQuery) OnlyX(ctx context.Context) *MatchesGateway {
	node, err := mgq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MatchesGateway ID in the query.
// Returns a *NotSingularError when more than one MatchesGateway ID is found.
// Returns a *NotFoundError when no entities are found.
func (mgq *MatchesGatewayQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mgq.Limit(2).IDs(setContextOp(ctx, mgq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{matchesgateway.Label}
	default:
		err = &NotSingularError{matchesgateway.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mgq *MatchesGatewayQuery) OnlyIDX(ctx context.Context) int {
	id, err := mgq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MatchesGateways.
func (mgq *MatchesGatewayQuery) All(ctx context.Context) ([]*MatchesGateway, error) {
	ctx = setContextOp(ctx, mgq.ctx, ent.OpQueryAll)
	if err := mgq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MatchesGateway, *MatchesGatewayQuery]()
	return withInterceptors[[]*MatchesGateway](ctx, mgq, qr, mgq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mgq *MatchesGatewayQuery) AllX(ctx context.Context) []*MatchesGateway {
	nodes, err := mgq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MatchesGateway IDs.
func (mgq *MatchesGatewayQuery) IDs(ctx context.Context) (ids []int, err error) {
	if mgq.ctx.Unique == nil && mgq.path != nil {
		mgq.Unique(true)
	}
	ctx = setContextOp(ctx, mgq.ctx, ent.OpQueryIDs)
	if err = mgq.Select(matchesgateway.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mgq *MatchesGatewayQuery) IDsX(ctx context.Context) []int {
	ids, err := mgq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mgq *MatchesGatewayQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mgq.ctx, ent.OpQueryCount)
	if err := mgq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mgq, querierCount[*MatchesGatewayQuery](), mgq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mgq *MatchesGatewayQuery) CountX(ctx context.Context) int {
	count, err := mgq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mgq *MatchesGatewayQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mgq.ctx, ent.OpQueryExist)
	switch _, err := mgq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mgq *MatchesGatewayQuery) ExistX(ctx context.Context) bool {
	exist, err := mgq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MatchesGatewayQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mgq *MatchesGatewayQuery) Clone() *MatchesGatewayQuery {
	if mgq == nil {
		return nil
	}
	return &MatchesGatewayQuery{
		config:     mgq.config,
		ctx:        mgq.ctx.Clone(),
		order:      append([]matchesgateway.OrderOption{}, mgq.order...),
		inters:     append([]Interceptor{}, mgq.inters...),
		predicates: append([]predicate.MatchesGateway{}, mgq.predicates...),
		// clone intermediate query.
		sql:  mgq.sql.Clone(),
		path: mgq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CompetitionName string `json:"competition_name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MatchesGateway.Query().
//		GroupBy(matchesgateway.FieldCompetitionName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mgq *MatchesGatewayQuery) GroupBy(field string, fields ...string) *MatchesGatewayGroupBy {
	mgq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MatchesGatewayGroupBy{build: mgq}
	grbuild.flds = &mgq.ctx.Fields
	grbuild.label = matchesgateway.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CompetitionName string `json:"competition_name,omitempty"`
//	}
//
//	client.MatchesGateway.Query().
//		Select(matchesgateway.FieldCompetitionName).
//		Scan(ctx, &v)
func (mgq *MatchesGatewayQuery) Select(fields ...string) *MatchesGatewaySelect {
	mgq.ctx.Fields = append(mgq.ctx.Fields, fields...)
	sbuild := &MatchesGatewaySelect{MatchesGatewayQuery: mgq}
	sbuild.label = matchesgateway.Label
	sbuild.flds, sbuild.scan = &mgq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MatchesGatewaySelect configured with the given aggregations.
func (mgq *MatchesGatewayQuery) Aggregate(fns ...AggregateFunc) *MatchesGatewaySelect {
	return mgq.Select().Aggregate(fns...)
}

func (mgq *MatchesGatewayQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mgq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mgq); err != nil {
				return err
			}
		}
	}
	for _, f := range mgq.ctx.Fields {
		if !matchesgateway.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mgq.path != nil {
		prev, err := mgq.path(ctx)
		if err != nil {
			return err
		}
		mgq.sql = prev
	}
	return nil
}

func (mgq *MatchesGatewayQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MatchesGateway, error) {
	var (
		nodes = []*MatchesGateway{}
		_spec = mgq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MatchesGateway).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MatchesGateway{config: mgq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mgq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (mgq *MatchesGatewayQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mgq.querySpec()
	_spec.Node.Columns = mgq.ctx.Fields
	if len(mgq.ctx.Fields) > 0 {
		_spec.Unique = mgq.ctx.Unique != nil && *mgq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mgq.driver, _spec)
}

func (mgq *MatchesGatewayQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(matchesgateway.Table, matchesgateway.Columns, sqlgraph.NewFieldSpec(matchesgateway.FieldID, field.TypeInt))
	_spec.From = mgq.sql
	if unique := mgq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mgq.path != nil {
		_spec.Unique = true
	}
	if fields := mgq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, matchesgateway.FieldID)
		for i := range fields {
			if fields[i] != matchesgateway.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mgq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mgq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mgq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mgq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mgq *MatchesGatewayQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mgq.driver.Dialect())
	t1 := builder.Table(matchesgateway.Table)
	columns := mgq.ctx.Fields
	if len(columns) == 0 {
		columns = matchesgateway.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mgq.sql != nil {
		selector = mgq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mgq.ctx.Unique != nil && *mgq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range mgq.predicates {
		p(selector)
	}
	for _, p := range mgq.order {
		p(selector)
	}
	if offset := mgq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mgq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MatchesGatewayGroupBy is the group-by builder for MatchesGateway entities.
type MatchesGatewayGroupBy struct {
	selector
	build *MatchesGatewayQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mggb *MatchesGatewayGroupBy) Aggregate(fns ...AggregateFunc) *MatchesGatewayGroupBy {
	mggb.fns = append(mggb.fns, fns...)
	return mggb
}

// Scan applies the selector query and scans the result into the given value.
func (mggb *MatchesGatewayGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mggb.build.ctx, ent.OpQueryGroupBy)
	if err := mggb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MatchesGatewayQuery, *MatchesGatewayGroupBy](ctx, mggb.build, mggb, mggb.build.inters, v)
}

func (mggb *MatchesGatewayGroupBy) sqlScan(ctx context.Context, root *MatchesGatewayQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mggb.fns))
	for _, fn := range mggb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mggb.flds)+len(mggb.fns))
		for _, f := range *mggb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mggb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mggb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MatchesGatewaySelect is the builder for selecting fields of MatchesGateway entities.
type MatchesGatewaySelect struct {
	*MatchesGatewayQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mgs *MatchesGatewaySelect) Aggregate(fns ...AggregateFunc) *MatchesGatewaySelect {
	mgs.fns = append(mgs.fns, fns...)
	return mgs
}

// Scan applies the selector query and scans the result into the given value.
func (mgs *MatchesGatewaySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mgs.ctx, ent.OpQuerySelect)
	if err := mgs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MatchesGatewayQuery, *MatchesGatewaySelect](ctx, mgs.MatchesGatewayQuery, mgs, mgs.inters, v)
}

func (mgs *MatchesGatewaySelect) sqlScan(ctx context.Context, root *MatchesGatewayQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mgs.fns))
	for _, fn := range mgs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mgs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mgs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
